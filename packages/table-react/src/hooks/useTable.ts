/**
 * useTable — the single entry point (SPEC §8).
 *
 * Target API:
 *   const table = useTable({ data, columns, getRowId });
 *   return <Table table={table} />;
 *
 * Advanced users pull headless primitives from the same object:
 *   table.getRenderColumns(), table.getRows(), ...
 *
 * Phase 2+ implements this on top of @softium/table-core derivations.
 */

export {};
