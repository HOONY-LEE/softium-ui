/**
 * App-shell primitives — the full @softium/ui surface, with the original
 * names (this entry's `Header` is the app-shell bar; the root entry renames
 * it to `AppHeader` to avoid clashing with the table's header).
 */
export * from '@softium/ui';
